


  <%= @user.name %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<%= link_to 'Appointments', users_path %>
<%# <%= link_to 'Favorites, users_path %> %>

<!-- <p>my favorites</p>

<ul>


  <% @user.favorites.each do |favorite| %>
  <% byebug %>
    <li><%=link_to favorite.name, subject_path(subject)%></li>
  <% end%>
</ul> -->
